import { Center, Text, View } from "native-base";
import React from "react";

export default function AccountsList(){
    return <View flex={1}>
        <Center w={[]}>

        </Center>
    </View>
}